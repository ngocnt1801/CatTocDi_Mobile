<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="12dp"
    android:paddingHorizontal="12dp"
    android:clipToPadding="false">
    <TextView
        android:id="@+id/promotion_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Widget.ActionBar.Subtitle"
        android:textColor="#000"
        android:text="Cắt tóc nam"/>
    <TextView
        android:id="@+id/service_price_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/promotion_title"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Caption"
        android:text="20,000 trong 20 phút"/>

    <Button
        android:id="@+id/edit_service_btn"
        android:layout_width="wrap_content"
        android:layout_height="28dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="@drawable/border_radius_outline_primary_fill_white"
        android:stateListAnimator="@drawable/click_effect"
        style="@style/MyButton"
        android:textAllCaps="true"
        android:text="Sửa"
        android:textColor="@color/darkTextColor"
        android:textSize="12dp"
        />
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#eee"
        android:layout_below="@id/service_price_time"
        android:layout_marginTop="12dp"
        />

</RelativeLayout>
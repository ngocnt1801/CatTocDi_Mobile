<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/firstColor</item>
        <item name="colorPrimaryDark">@color/mainDark</item>
        <item name="colorAccent">@color/icLogin</item>
    </style>

    <style name="SplashStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/splash_gradient_background</item>
    </style>

    <style name="RatingSmall" parent="Base.Widget.AppCompat.RatingBar.Small">
        <item name="android:process">@color/secondLight</item>
        <item name="android:secondaryProgress">@color/secondPrimary</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
